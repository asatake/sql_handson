<h1>Edit Article tag</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.article_tag_path(@conn, :update, @article_tag)) %>

<span><%= link "Back", to: Routes.article_tag_path(@conn, :index) %></span>
